# Normalise line endings so cross-platform (Windows/macOS/Linux) git users
# don't get CRLF/LF churn. Scripts stay LF; PowerShell stays CRLF; assets binary.
* text=auto eol=lf
*.ps1 text eol=crlf
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
